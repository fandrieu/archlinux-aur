# vim:set ft=sh:

build ()
{
    MODULES="aufs squashfs loop"
    BINARIES=""
    FILES=""
    SCRIPT="rootaufs"
}

help ()
{
cat <<HELPEOF
  This hook will enable booting from an aufs root filesystem.
  Requires "aufs" module.
  Needs "fsck" hook to check root fs.
HELPEOF
}
